import Foundation

extension Strings {
    // MARK: - Data (the combined import/export screen)

    enum Data {
        static var title: String { String(localized: "data.title") }
        static var exportSection: String { String(localized: "data.export_section") }
        static var importSection: String { String(localized: "data.import_section") }
    }
}
